# temp-frontend (TEMPORARY)

⚠️ **This is throwaway scaffolding**, not the real app structure.

A minimal Next.js (App Router) prototype to test the flow:
Auth0 login → paste a video URL → see (stubbed) reposts.

To run:

```bash
cd temp-frontend
npm install
npm run dev
```

Env vars live in `temp-frontend/.env` (gitignored). Will be reorganized later.
